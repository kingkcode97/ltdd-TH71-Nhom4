<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.FourFashionShop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight">
        <activity android:name=".Activities.ProductDetailsActivity"></activity>
        <activity android:name=".Activities.CategoryActivity" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SplashActivity"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.UserInfoActivity" />
        <activity android:name=".Activities.SupportActivity" />
        <activity android:name=".Activities.LocationUserActivity" />
        <activity android:name=".Activities.BeforeOrderActivity" />
        <activity android:name=".Activities.OrderInfoActivity" />
        <activity android:name=".Activities.OrdersManagementActivity" />
        <activity android:name=".Activities.FavoriteProductActivity" />
        <activity android:name=".Activities.ProductsPurchasedActivity" />
        <activity android:name=".Activities.ResetPasswordActivity" />
        <activity
            android:name=".Activities.MainActivity"
            android:theme="@style/BottomNavigation" />
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".Activities.RegisterActivity"
            android:theme="@style/AppTheme.FullScreen" />
    </application>

</manifest>