<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.FourFashionShop">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.FullScreen">
        <activity
            android:theme="@style/Theme.AppCompat.DayNight"
            android:name=".ui.OrdersManagementActivity"/>
        <activity
            android:name=".ui.FavoriteProductActivity"
            android:theme="@style/Theme.AppCompat.DayNight" />
        <activity
            android:name=".ui.ProductsPurchasedActivity"
            android:theme="@style/Theme.AppCompat.DayNight" />
        <activity
            android:name=".ui.SupportActivity"
            android:theme="@style/Theme.AppCompat.DayNight" />
        <activity android:name=".ui.Mall_Product_Activity" />
        <activity
            android:name=".ui.UserInfoActivity"
            android:theme="@style/Theme.AppCompat.DayNight" />
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/BottomNavigation" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.RegisterActivity" />
        <activity android:name=".ui.WelcomeActivity" />
        <activity android:name=".ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>